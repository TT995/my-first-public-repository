# my-first-public-repository

# Git 強制同期系コマンド集

## origin/main の状態へ完全リセット

ローカル変更をすべて破棄し、GitHub 上の `origin/main` と完全一致させる。

```bash
git fetch origin
git reset --hard origin/main
git clean -fd
```

- staged (`Changes to be committed`) を破棄
- unstaged (`Changes not staged for commit`) を破棄
- untracked file も削除
- ローカルを `origin/main` と完全一致

---

## 作業ブランチを local main に強制追従

現在の作業ブランチへ `main` を取り込み、コンフリクト時は `main` 側を優先する。

```bash
git merge main -X theirs
```

- コンフリクト時に `main` の内容を採用
- 作業ブランチを `main` 寄りに同期

---

## GitHub に push した commit を消す（履歴改変）

直前の commit を消し、その状態を GitHub に強制反映する。

```bash
git reset --hard HEAD~1
git push --force origin main
```

- `HEAD~1`
  = 1 commit 前へ戻る
- GitHub 側の履歴も上書きされる
- 共同開発では危険

### 安全版（履歴を壊さない）

```bash
git revert HEAD
git push origin main
```

- 打ち消し commit を追加
- 履歴は保持される
- チーム開発向け





# `@AuthenticationPrincipal Jwt jwt` の使い方

## 概要

```java
@AuthenticationPrincipal Jwt jwt
```

と書くと、Spring Security が現在ログイン中ユーザーの JWT を controller 引数に自動注入する。

内部的には `SecurityContextHolder` から取得している。

---

# 使用例

```java
@RestController
public class PostController {

    @PostMapping("/posts")
    public void createPost(
            @AuthenticationPrincipal Jwt jwt,
            @RequestBody CreatePostRequest request
    ) {

        String userId = jwt.getSubject();

        System.out.println(userId);

        System.out.println(request.text());
    }
}
```

---

# 何が起きているか

クライアント:

```http
POST /posts
Authorization: Bearer eyJ...
```

↓

Spring Security が access token(JWT) を検証

↓

`Authentication` を生成

↓

`SecurityContextHolder` に保存

↓

`@AuthenticationPrincipal` が JWT を controller 引数へ注入

---

# よく使うメソッド

## ユーザーID(sub)

```java
jwt.getSubject()
```

---

## claim取得

```java
jwt.getClaim("email")
```

```java
jwt.getClaim("cognito:username")
```

---

# 内部的にやっていること

以下を Spring が自動化している。

```java
Authentication auth =
    SecurityContextHolder.getContext()
        .getAuthentication();

Jwt jwt = (Jwt) auth.getPrincipal();
```

---

# 利点

- 毎回 `SecurityContextHolder` を書かなくてよい
- 現在ユーザーを簡単に取得できる
- userId を request body に含める必要がない
- JWT認証との相性が良い

---

# 注意

通常、ユーザー識別は JWT から行う。

危険:

```json
{
  "userId": "admin"
}
```

安全:

```java
String userId = jwt.getSubject();
```

# search

```java
@Repository
@RequiredArgsConstructor
public class SearchRepository {

    private final NamedParameterJdbcTemplate jdbcTemplate;

    public List<Post> getTargetPosts(
            String key,
            List<Integer> tagIdList
    ) {

        StringBuilder sql = new StringBuilder("""
            SELECT DISTINCT p.*
            FROM post p
            LEFT JOIN post_tag pt
                ON p.id = pt.post_id
            WHERE 1 = 1
        """);

        MapSqlParameterSource params = new MapSqlParameterSource();

        // key の部分一致
        if (key != null && !key.isBlank()) {

            sql.append("""
                AND p.content ILIKE :key
            """);

            params.addValue("key", "%" + key + "%");
        }

        // tag の OR 検索
        if (tagIdList != null && !tagIdList.isEmpty()) {

            sql.append("""
                AND pt.tag_id IN (:tagIds)
            """);

            params.addValue("tagIds", tagIdList);
        }

        return jdbcTemplate.query(
                sql.toString(),
                params,
                (rs, rowNum) -> new Post(
                        rs.getInt("id"),
                        rs.getString("content")
                )
        );
    }
}
```

```java
@RestController
@RequiredArgsConstructor
@RequestMapping("/api")
public class SearchController {

    private final SearchRepository searchRepository;

    @GetMapping("/search")
    public List<Post> getTargetPosts(
            @RequestParam(required = false) String key,

            @RequestParam(
                    name = "tag",
                    required = false
            )
            List<Integer> tagIdList
    ) {

        return searchRepository.getTargetPosts(
                key,
                tagIdList
        );
    }
}
```

# メモ

- `/api/search?key=Java&tag=1&tag=2`
  のような URL は自然

- `tag=1&tag=2`
  は Spring が `List<Integer>` に自動変換

- `key=` は空文字 `""`
  `key` 自体が無い場合は `null`

- `WHERE 1 = 1`
  は後ろに `AND` を機械的に追加


---





# 気が利く

## 事象
既存のスケジュールはすでに組まれていたが、既存プロダクトのデバッグや未知の課題が後から多発するリスクを察知した。そこで、事前にチーム全体へ「現状抱えている懸念点やバグ」をヒアリングし、先回りしてそれらをバッファとしてタスクに組み込んだ。問題が表面化する前に現場の不安を吸い上げ、計画に織り込んだことがスケジュールの破綻を防ぐ結果に繋がった。

## そのとき考えたこと
「組まれた予定通りに進める」だけでは、必ず発生する不測のエラーに対処できずチームの進行が止まると考えた。問題が起きてから対処する「事後対応」ではなく、起きうる問題をあらかじめ予測して計画に織り込む「先回り」こそが、チーム全体のアウトカムを最大化するために自分が取るべき行動だと判断した。

## 教訓
「予定を立てることと、予定通り進めるための準備をすることは別である」と学んだ。今後はスケジュールを作成する際、必ず「予見されるリスクやバグ修正のバッファ」をあらかじめチームから吸い上げてタスク化するプロセスを組み込み、この先回りの動きをチーム全体で再現していく。

---

## 事象
指示されたタスクに対し、良かれと思って頼まれていない機能や装飾（自分なりのアレンジ）を追加してしまった。しかし結果的にそれはレビューアの意図とは異なっており、「まずは言われた要件を100％満たす」という原点から逸脱し、手戻りを発生させた。

## そのとき考えたこと
当時は「期待値を超えること」が良い仕事だと考え、自分なりの付加価値を加えることを優先してしまった。しかし、本当に求められていたのは独自の工夫ではなく、まず事実として与えられた要件を正確に実現することだった。期待値を超えることと独りよがりな追加作業は全く別物であり、その違いを十分に認識できていなかった。

## 教訓
「気を利かせる前に、まず相手の期待を正確に満たすことが重要である」と学んだ。今後は追加提案や改善案を考える際も、まずは要件を100％満たした状態を作った上で実施し、独断で仕様を変更しないことを徹底する。

# キャッチアップ

## 事象
発熱により講義を欠席した。復帰後、自身の遅れを取り戻すため、同期にノートを共有してもらうだけでなく、周囲のメンバーや有識者に対し「自分が休んだ範囲の重要ポイント」を積極的に質問して回った。遠慮せずに自ら情報を引き出しに行ったことで、一人で資料を読むだけでは得られない決定事項の背景まで深く理解し、即座に合流することができた。

## そのとき考えたこと
即戦力として現場に立つためには、一人で悩む時間を1秒でも削るべきだと考えた。相手の時間を奪う遠慮よりも、自分がチームのボトルネックになり続けるリスクを重く見て、プライドを捨てて周囲の知識を借りに行く当事者意識が必要だと判断した。

## 教訓
「知らなかった事実を放置せず、自ら情報を取りに行く姿勢」の重要性を学んだ。今後も未経験領域や遅れが発生した際は、一人で抱え込まずに有識者へ積極的に相談し、最短距離でキャッチアップを行う。

# やり遂げる

## 事象
振り返りアプリのテストデータ作成時、どのようなデータが適切か結論が出ず、適当な文字列を投入してタスクを完了させてしまった。結果としてアプリの空気感が伝わらず、後工程のUI検証に支障をきたした。「どうすればユーザーに価値が伝わるか」という意図を持たず、自分の作業を終わらせることだけを優先したことが失敗の要因だった。

## そのとき考えたこと
要件が曖昧な中で正解を探すことに疲れ、タスクをクローズすること自体が目的になってしまった。本来重視すべきはアウトプットではなくアウトカムであり、その思考を放棄した結果、中身のない成果物を生み出してしまったと反省した。

## 教訓
「意図のないアウトプットは、チームにとって無価値である」と学んだ。今後は正解のないタスクでも適当に終わらせず、自分なりの仮説や意図を持った上でチームへ相談し、成果に執着して最後までやり遂げる。

---

## 事象
チームで「No.1」という目標を掲げていたが、現在のプロダクトコンセプトでは達成が非現実的であることに気付き、途中で「Top10」へ下方修正した。本気でNo.1を目指すのであれば、要件定義の段階でその目標に耐えうるコンセプトを練り込むべきだった。

## そのとき考えたこと
当初は高い目標を掲げること自体に満足し、それを実現するための戦略や検証が不足していた。目標達成に必要な事実やロジックを伴わない願望レベルの目標設定だったことが失敗の原因だった。

## 教訓
「目標とは願望ではなく、達成に向けた戦略とセットである」と学んだ。今後は目標設定時に必ずロードマップを描き、実現可能性を検証した上で目標を定める。

---

## 事象
プロダクト開発に全力を注ぎ、目標としていた機能を完成させることはできた。しかし開発作業に時間を使い過ぎた結果、成果をステークホルダーへ伝えるためのプレゼン準備がおろそかになった。発表では聞き手の前提知識に合わせた説明ができず、機能の魅力やビジネス上の意図が十分に伝わらなかった。

## そのとき考えたこと
「良いものを作れば自然と価値が伝わる」と考え、成果を相手に理解させる工程を軽視していた。ビジネスで求められるのは開発した事実ではなく、その成果によって相手を動かすことであるにもかかわらず、自分視点で物事を進めてしまった。

## 教訓
「どれだけ優れた成果物でも、相手に伝わらなければ価値は生まれない」と学んだ。今後は開発と説明準備を一体で考え、「誰に何を伝え、どう動いてほしいか」を成果物の完了条件に組み込む。

# 段取り

## 事象
毎日の進捗報告タスクに対し、Slackのスケジュールリマインド機能を設定し、毎日定刻に通知が飛ぶ仕組みを構築した。その結果、研修期間を通じて報告漏れゼロを達成できた。

## そのとき考えたこと
人間は必ずミスをするため、「忘れないように気を付ける」という精神論では再現性がない。意志の力に依存せず、自動的にタスクを実行できる環境を作ることが重要だと考えた。

## 教訓
「気合いでミスを防ぐのではなく、仕組みでミスを防ぐことが段取りである」と学んだ。今後も定常業務はリマインドや自動化を活用し、ミスの発生を構造的に防止する。

---

## 事象
沖縄合宿後に1週間の講義期間があり開発作業が停止することが事前に分かっていたにもかかわらず、その制約を加味した進行計画を立てられていなかった。結果として評価期間前半に不完全なプロダクトを提示することになり、チームの成果を十分に伝えられなかった。

## そのとき考えたこと
「講義期間は仕方がない」と外部要因を理由にしていたが、本来は制約を見越して計画を立てるべきだった。開発が止まることが分かっていた以上、事前にMVPを完成させるなどの対応が必要だった。

## 教訓
「カレンダーを見ることと、制約を織り込んで段取りを組むことは別である」と学んだ。今後は作業停止期間や外部要因を初期段階で洗い出し、逆算したマイルストーン設計を徹底する。


# 気が利く

## 事象
既存のスケジュールはすでに組まれていたが、既存プロダクトのデバッグや未知の課題が後から多発するリスクを察知した。そこで、事前にチーム全体へ「現状抱えている懸念点やバグ」をヒアリングし、先回りしてそれらをバッファとしてタスクに組み込んだ。問題が表面化する前に現場の不安を吸い上げ、計画に織り込んだことがスケジュールの破綻を防ぐ結果に繋がった。

## そのとき考えたこと
「組まれた予定通りに進める」だけでは、必ず発生する不測のエラーに対処できずチームの進行が止まると考えた。問題が起きてから対処する「事後対応」ではなく、起きうる問題をあらかじめ予測して計画に織り込む「先回り」こそが、チーム全体のアウトカムを最大化するために自分が取るべき行動だと判断した。

## 教訓
「予定を立てることと、予定通り進めるための準備をすることは別である」と学んだ。今後はスケジュールを作成する際、必ず「予見されるリスクやバグ修正のバッファ」をあらかじめチームから吸い上げてタスク化するプロセスを組み込み、この先回りの動きをチーム全体で再現していく。

---

## 事象
指示されたタスクに対し、良かれと思って頼まれていない機能や装飾（自分なりのアレンジ）を追加してしまった。しかし結果的にそれはレビューアの意図とは異なっており、「まずは言われた要件を100％満たす」という原点から逸脱し、手戻りを発生させた。

## そのとき考えたこと
当時は「期待値を超えること」が良い仕事だと考え、自分なりの付加価値を加えることを優先してしまった。しかし、本当に求められていたのは独自の工夫ではなく、まず事実として与えられた要件を正確に実現することだった。期待値を超えることと独りよがりな追加作業は全く別物であり、その違いを十分に認識できていなかった。

## 教訓
「気を利かせる前に、まず相手の期待を正確に満たすことが重要である」と学んだ。今後は追加提案や改善案を考える際も、まずは要件を100％満たした状態を作った上で実施し、独断で仕様を変更しないことを徹底する。

# キャッチアップ

## 事象
発熱により講義を欠席した。復帰後、自身の遅れを取り戻すため、同期にノートを共有してもらうだけでなく、周囲のメンバーや有識者に対し「自分が休んだ範囲の重要ポイント」を積極的に質問して回った。遠慮せずに自ら情報を引き出しに行ったことで、一人で資料を読むだけでは得られない決定事項の背景まで深く理解し、即座に合流することができた。

## そのとき考えたこと
即戦力として現場に立つためには、一人で悩む時間を1秒でも削るべきだと考えた。相手の時間を奪う遠慮よりも、自分がチームのボトルネックになり続けるリスクを重く見て、プライドを捨てて周囲の知識を借りに行く当事者意識が必要だと判断した。

## 教訓
「知らなかった事実を放置せず、自ら情報を取りに行く姿勢」の重要性を学んだ。今後も未経験領域や遅れが発生した際は、一人で抱え込まずに有識者へ積極的に相談し、最短距離でキャッチアップを行う。

# やり遂げる

## 事象
振り返りアプリのテストデータ作成時、どのようなデータが適切か結論が出ず、適当な文字列を投入してタスクを完了させてしまった。結果としてアプリの空気感が伝わらず、後工程のUI検証に支障をきたした。「どうすればユーザーに価値が伝わるか」という意図を持たず、自分の作業を終わらせることだけを優先したことが失敗の要因だった。

## そのとき考えたこと
要件が曖昧な中で正解を探すことに疲れ、タスクをクローズすること自体が目的になってしまった。本来重視すべきはアウトプットではなくアウトカムであり、その思考を放棄した結果、中身のない成果物を生み出してしまったと反省した。

## 教訓
「意図のないアウトプットは、チームにとって無価値である」と学んだ。今後は正解のないタスクでも適当に終わらせず、自分なりの仮説や意図を持った上でチームへ相談し、成果に執着して最後までやり遂げる。

---

## 事象
チームで「No.1」という目標を掲げていたが、現在のプロダクトコンセプトでは達成が非現実的であることに気付き、途中で「Top10」へ下方修正した。本気でNo.1を目指すのであれば、要件定義の段階でその目標に耐えうるコンセプトを練り込むべきだった。

## そのとき考えたこと
当初は高い目標を掲げること自体に満足し、それを実現するための戦略や検証が不足していた。目標達成に必要な事実やロジックを伴わない願望レベルの目標設定だったことが失敗の原因だった。

## 教訓
「目標とは願望ではなく、達成に向けた戦略とセットである」と学んだ。今後は目標設定時に必ずロードマップを描き、実現可能性を検証した上で目標を定める。

---

## 事象
プロダクト開発に全力を注ぎ、目標としていた機能を完成させることはできた。しかし開発作業に時間を使い過ぎた結果、成果をステークホルダーへ伝えるためのプレゼン準備がおろそかになった。発表では聞き手の前提知識に合わせた説明ができず、機能の魅力やビジネス上の意図が十分に伝わらなかった。

## そのとき考えたこと
「良いものを作れば自然と価値が伝わる」と考え、成果を相手に理解させる工程を軽視していた。ビジネスで求められるのは開発した事実ではなく、その成果によって相手を動かすことであるにもかかわらず、自分視点で物事を進めてしまった。

## 教訓
「どれだけ優れた成果物でも、相手に伝わらなければ価値は生まれない」と学んだ。今後は開発と説明準備を一体で考え、「誰に何を伝え、どう動いてほしいか」を成果物の完了条件に組み込む。

# 段取り

## 事象
毎日の進捗報告タスクに対し、Slackのスケジュールリマインド機能を設定し、毎日定刻に通知が飛ぶ仕組みを構築した。その結果、研修期間を通じて報告漏れゼロを達成できた。

## そのとき考えたこと
人間は必ずミスをするため、「忘れないように気を付ける」という精神論では再現性がない。意志の力に依存せず、自動的にタスクを実行できる環境を作ることが重要だと考えた。

## 教訓
「気合いでミスを防ぐのではなく、仕組みでミスを防ぐことが段取りである」と学んだ。今後も定常業務はリマインドや自動化を活用し、ミスの発生を構造的に防止する。

---

## 事象
沖縄合宿後に1週間の講義期間があり開発作業が停止することが事前に分かっていたにもかかわらず、その制約を加味した進行計画を立てられていなかった。結果として評価期間前半に不完全なプロダクトを提示することになり、チームの成果を十分に伝えられなかった。

## そのとき考えたこと
「講義期間は仕方がない」と外部要因を理由にしていたが、本来は制約を見越して計画を立てるべきだった。開発が止まることが分かっていた以上、事前にMVPを完成させるなどの対応が必要だった。

## 教訓
「カレンダーを見ることと、制約を織り込んで段取りを組むことは別である」と学んだ。今後は作業停止期間や外部要因を初期段階で洗い出し、逆算したマイルストーン設計を徹底する。

